<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apkatrening">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".PlanyDniaActivity"></activity>
        <activity
            android:name=".PopConfirmProsbaDelete"
            android:theme="@style/AppTheme.PopUp" />
        <activity
            android:name=".PopConfirmWymiarDelete"
            android:theme="@style/AppTheme.PopUp" />
        <activity android:name=".AddWymiarActivity" />
        <activity
            android:name=".PopChangePass"
            android:theme="@style/AppTheme.PopUp" />
        <activity
            android:name=".PopConfirmProsbaActivity"
            android:theme="@style/AppTheme.PopUp" />
        <activity android:name=".EditDaneActivity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".DashboardActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>